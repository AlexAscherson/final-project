
  <div class="slider">
        <ul>
          <% @tournament.users.each do | user | %>
            <li>
              <% profileimg = user.profile_image_url %> 
              <%= image_tag(profileimg, :alt => "rss feed", :class => "profile_thumb") %>

              </li>
          <% end %>
        </ul>
        </div>

