<%= form_tag charges_path do %>
  <article>
    <label class="Stake">
      <span>Stake to Enter: £<%= @tournament.stake %></span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="Stake for <% @tournament.name %>"
          data-amount="<% @tournament.stake %>">
          </script>
<% end %>

