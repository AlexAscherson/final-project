<% if @event.users.exists?(current_user.id) %>
  
   $('.joinevent').html("<%= escape_javascript(button_to 'Leave Event', joinevent_group_tournament_event_path, :class =>"btn btn-primary btn-lg", id: "joinevent",  remote: true, method: :post ) %>") 
   $('.membersheader').html("<%= escape_javascript(render partial:'eventusers') %>")
   console.log("memberalready");
    <% else %>
  
   $('.joinevent').html("<%= escape_javascript(button_to 'Join Event', joinevent_group_tournament_event_path, :class =>"btn btn-primary btn-lg", id: "joinevent",  remote: true, method: :post ) %>") 
   $('.membersheader').html("<%= escape_javascript(render partial:'eventusers') %>")
   console.log("memberalready");
     <%  end %>
